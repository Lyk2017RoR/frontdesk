<div class="row">
  <div class="col-lg-12 text-center">
    <h1>Anasayfa</h1>
    <p class="lead">Find me in app/views/frontpage/index.html.erb</p>

    <ul class="nav pull-right">
      <li>
        <div id="user_nav">
          <% if user_signed_in? %>
              <%= current_user.email %> </br>
              <%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => 'btn btn-danger btn-mini' %>
          <% else %>
              <%= link_to new_user_registration_path do %>
                  <button class="btn-mini btn btn-info">Sign Up</button>
              <% end %>
              </br></br> <%= link_to new_user_session_path do %>
                  <button class="btn btn-info btn-mini">Sign In</button>
              <% end %>
          <% end %>
        </div>
      </li>
    </ul>

  </div>
</div>
